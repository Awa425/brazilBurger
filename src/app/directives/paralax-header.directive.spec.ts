import { ParalaxHeaderDirective } from './paralax-header.directive';

describe('ParalaxHeaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ParalaxHeaderDirective();
    expect(directive).toBeTruthy();
  });
});
